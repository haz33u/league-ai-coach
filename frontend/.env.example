# Backend API URL
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# Application Environment
NEXT_PUBLIC_API_MODE=development
NODE_ENV=development

# ⚠️ SECURITY WARNING ⚠️
# NEVER add RIOT_API_KEY here!
# API keys must ONLY be in backend environment variables.
# Frontend should NEVER have direct access to Riot API keys.
